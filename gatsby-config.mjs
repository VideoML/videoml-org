const config = {
  siteMetadata: {
    title: "VideoML - Draft Specification",
    description: "VideoML: An XML-based markup language for declarative video composition",
    siteUrl: "https://videoml.org",
  },
  graphqlTypegen: false,
  plugins: [],
};

export default config;
